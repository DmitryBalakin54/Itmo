pi{StudentId, StudentName, GroupId}(s{CourseName = :CourseName}(Students njoin Courses njoin Plan)) diff pi{StudentId, StudentName, GroupId}(s{CourseName = :CourseName}(Students njoin Marks njoin Courses))